const Heading = ({ className }) => {
  return (
    <h1 className={`${className}  h-fit font-poppins `}>
      The Ultimate <span className="text-linear">CS 2 Marketplace</span> for
      Gamers Who Mean Business
    </h1>
  );
};

export default Heading;
